services:
  - type: web
    name: artvoice
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: REPLICATE_API_TOKEN
        sync: false
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
